content:
  - name: posts
    label: Articles
    path: Data/Articals.json
    format: json
    fields:
      - name: Title
        type: string
      - name: Category
        type: select
        options: [Sports,Science,Politics]
      - name: Date
        type: date
