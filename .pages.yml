media: files

content:
  - name: AboutAuthor
    type: file
    path: _data/AboutAuthor.json
    fields:
      - name: Name
        type: string

      - name: Expertise
        type: string

      - name: Pic
        type: image
        
      - name: Desciption
        type: rich-text
        options:
          switcher: false

  - name: Books
    path: _data/BooksData
    type: collection
    format: json
    filename: "{slug}.json"
    view:
      fields: [Name, Pic, Topic, PublishedDate, Link]
      primary: Name
      sort: [PublishedDate, Name, Topic]
      default:
        sort: PublishedDate
        order: desc

    fields:
      - name: Name
        type: string
        required: true
      
      - name: PublishedDate
        type: date
        required: true
        
      - name: Pic
        type: image

      - name: Topic
        type: string

      - name: About
        type: rich-text
        options:
          switcher: false

      - name: Link
        type: string

  - name: ArticleCategories
    type: file
    path: _data/ArticleCategories.json
    fields:
      - name: categories
        type: string
        list: true

  - name: Articles
    path: _data/ArticlesData
    type: collection
    format: json
    filename: "{primary}.json"
    
    view:
      fields: [Title, Topic, Published, ReleaseDate, LastUpdated]
      primary: Title
      sort: [ReleaseDate, Title]
      default:
          sort: ReleaseDate
          order: desc
        
    fields:
      - name: Title
        type: string
        required: true
        
      - name: Topic
        type: select
        required: true
        options:
          values:
            - name: ArticleCategories
              type: reference
              options:
                collection: ArticleCategories
          placeholder: Select the category of the Artical
        
      - name: Published
        type: boolean
        default: false

      - name: Comments
        type: boolean
        default: true
        
      - name: Likes
        type: boolean
        default: true

      - name: ReadTime
        type: number
        options:
          min: 1
        
      - name: Description
        type: rich-text
        options:
          switcher: false
        
      - name: Body
        type: rich-text
        required: true
        options:
          switcher: false
        
      - name: ReleaseDate
        type: date
        required: true
        
      - name: LastUpdated
        type: date
        
  