content:
  - name: posts
    label: Articles
    type: file
    path: docs
    fields:
      - name: Title
        type: string
      - name: Category
        type: select
        options: ["Sports", "Politics", "Science"]
      - name: Date
        type: date
