content:
  - name: posts
    label: Articles
    type: file
    path: docs
    fields:
      - name: Title
        type: string
      - name: Category
        type: select
        options: {Sports,Science,Politics}
      - name: Date
        type: date
